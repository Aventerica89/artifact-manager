{
  "name": "artifact-manager-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250115.0",
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8",
    "wrangler": "^3.102.0"
  }
}
